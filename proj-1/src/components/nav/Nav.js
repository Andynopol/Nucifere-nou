import React from 'react';
import NavMenu from './NavMenu';

export default class Nav extends React.Component{
    render(){
        return(
            <nav>
                <NavMenu />
            </nav>
        );
    }
}